#include <string.h>
#include <stdio.h>

int cat_strings    (char*, char*, char*);
int cat_length     (char*, char*);
int nc_create_file (char*);

#ifndef MM_UTIL
#define MM_UTIL
#include "util.c"
#endif
